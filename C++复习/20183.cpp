class PolyN
{
    PolyN(int n,int cof[])
    {
        sizeN=n;
        for(int i=0;i<n;i++)
            coef[i]=cof[i];
    }
    double caculate(double x)
    {
        double res;
        for(int i=0;i<sizeN;i++)
        {
            double temp=1;
            for(int i=0;i<coef[i];i++)
        }
    }
    private:
        double x;
         int sizeN;
        int coef[2];
}